**Fixed** — The `repository` URL in `Cargo.toml` pointed at
`thekevinbot/template-lib`, a name the repo no longer uses. It now names
`thekevinscott/template-lib`, which is what the crates.io trusted-publisher
path compares against the OIDC source claim.
