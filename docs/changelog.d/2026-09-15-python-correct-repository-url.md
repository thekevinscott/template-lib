**Fixed** — The `Repository` URL in `pyproject.toml` pointed at
`thekevinbot/template-lib`, a name the repo no longer uses. It now names
`thekevinscott/template-lib`, which is what PyPI's trusted-publisher path
compares against the OIDC source claim.
