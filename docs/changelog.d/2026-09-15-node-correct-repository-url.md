**Fixed** — The `repository` URL in `package.json` pointed at
`thekevinbot/template-lib`, a name the repo no longer uses. It now names
`thekevinscott/template-lib`, which is what npm's provenance check compares
against the OIDC source claim.
